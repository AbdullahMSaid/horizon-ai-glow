## Convert to Vite React SPA — plan + blockers

I can do steps 1, 2, 4, 5, 6, 7, 8, 9, 10 mechanically. But steps 3 and "don't touch `src/routes/`" together **will not produce a working app**. Flagging before you approve.

### Blockers in the request as written

1. **`src/routes/__root.tsx` is TanStack Start-only.** It uses `shellComponent`, `HeadContent`, `Scripts`, and `createRootRouteWithContext` — all from `@tanstack/react-start` / the Start router. In a plain SPA these don't exist; the router won't construct. To keep `@tanstack/react-router` (per your instruction) but drop Start, `__root.tsx` must be rewritten to a plain `createRootRoute({ component: ... })` with an `<Outlet />`, and `<head>`/scripts move into `index.html`. **I have to edit this file** — it's in `src/routes/` which you said not to touch.

2. **Route `head()` metadata.** `src/routes/founder.tsx` and `src/routes/index.tsx` likely use the Start `head()` option for `<title>`/meta. That option is ignored by plain `@tanstack/react-router`. SEO meta needs to go into `index.html` (static) or a client-side effect. Without changes the pages still render, but titles/meta become static.

3. **`src/main.tsx` doesn't exist.** Step 3 says "ensure it renders" — I'll create it. Note: it imports `./index.css`, but the project's stylesheet is `src/styles.css`. I'll import `./styles.css` instead unless you want me to rename.

4. **`index.html` doesn't exist at the project root.** Step 10 says "verify" — I'll create a full minimal one (doctype, head with title/meta + Google Fonts + favicon links currently in `__root.tsx`, body with `<div id="root">` and the module script).

5. **Server-side Supabase files become dead code and will fail typecheck on import.** `src/integrations/supabase/auth-attacher.ts`, `auth-middleware.ts`, `client.server.ts`, and `src/start.ts` all import from `@tanstack/react-start`. If we remove `@tanstack/react-start` from deps, these files won't compile. They're not referenced by your components (only by Start's runtime), so I'll **delete `src/start.ts` and the three server-side Supabase files**. The browser `client.ts` stays. Tell me if anything in your components uses them — a quick grep says no, but confirm.

6. **`@lovable.dev/vite-tanstack-config` and Lovable platform fit.** The Lovable preview/build pipeline is built around TanStack Start. Converting to a plain Vite SPA will likely break the in-editor preview here even if it deploys fine on Cloudflare Pages. You'll be deploying from your own GitHub fork to Cloudflare, not via Lovable's deploy. Confirm you're OK with the Lovable preview potentially breaking.

7. **Dependencies to also remove** (otherwise dead but installed): `@tanstack/react-start`, `@tanstack/router-plugin`, `@cloudflare/vite-plugin`, `@lovable.dev/vite-tanstack-config`. You said "only remove `@tanstack/react-start`" — keeping the others is fine (they just sit unused), but `@lovable.dev/vite-tanstack-config` is no longer imported so it's harmless. I'll leave them per your instruction.

8. **`routeTree.gen.ts`** is generated by `@tanstack/router-plugin`. With the new `vite.config.ts` it won't regenerate. That's fine as long as you don't add/rename routes — the current file is checked in and will keep working. If you later add a route, you'll need to add the router plugin back.

### Files I'll change

- `package.json` — `sideEffects: true`, drop `nitro`, build script = `vite build`. (Also drop `@tanstack/react-start` per step "only remove @tanstack/react-start".)
- `vite.config.ts` — replace with your provided config.
- `src/main.tsx` — create (with `./styles.css` import, not `./index.css`).
- `index.html` — create at project root with `<div id="root">`, module script, and the head tags currently in `__root.tsx` (title, meta, fonts, favicon).
- `src/routes/__root.tsx` — rewrite as plain `createRootRoute` with `<Outlet />`. **Required despite your "don't touch routes" rule.**
- `public/_redirects` — already has the exact contents you specified; no change needed.
- Delete: `wrangler.jsonc`, `netlify.toml`, `src/server.ts`, `src/lib/error-capture.ts`, `src/lib/error-page.ts`, `src/start.ts`, `src/integrations/supabase/auth-attacher.ts`, `src/integrations/supabase/auth-middleware.ts`, `src/integrations/supabase/client.server.ts`.

### Cloudflare Pages settings after this

- Build command: `bun run build` (or `npm run build`)
- Output directory: `dist`
- Env vars (build-time, baked into client): `VITE_SUPABASE_URL`, `VITE_SUPABASE_PUBLISHABLE_KEY`, `VITE_SUPABASE_PROJECT_ID`
- No Functions, no Worker — pure static.

### Two things I need you to confirm

1. OK to edit `src/routes/__root.tsx` (required) and to also delete `src/start.ts` + the three server-side Supabase files (required to typecheck)?
2. OK that the in-Lovable preview may stop working after this conversion?
